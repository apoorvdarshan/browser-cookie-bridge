{
  "name": "brave-codex-cookie-sync",
  "version": "0.4.0",
  "description": "Local Chromium-browser to Codex data sync for macOS",
  "type": "module",
  "bin": {
    "brave-codex-cookie-sync": "./bin/brave-codex-cookie-sync.js"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check bin/brave-codex-cookie-sync.js && node --check src/app-installer.js && node --check src/broker.js && node --check src/cli.js && node --check src/config.js && node --check src/paths.js && node --check src/scheduler.js && node --check extension-template/background.js",
    "build:app": "node bin/brave-codex-cookie-sync.js install-app --no-open"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "files": [
    "bin",
    "src",
    "extension-template",
    "macos-app/Package.swift",
    "macos-app/Info.plist",
    "macos-app/Resources",
    "macos-app/Sources",
    "README.md",
    "LICENSE"
  ]
}
